<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Login Page</title>
    <style>
        /* Reset some default browser styling */
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: Arial, sans-serif;
            background-color: #f4f4f4;
            padding-top: 60px; /* Adjust if your header height differs */
            padding-bottom: 60px; /* Adjust if your footer height differs */
        }

        .login-wrapper {
            display: flex;
            justify-content: space-between;
            align-items: center;
            background-color: #fff;
            padding: 30px;
            border-radius: 10px;
            box-shadow: 0px 4px 12px rgba(0, 0, 0, 0.1);
            max-width: 900px;
            margin: 50px auto;
        }

        .login-container {
            width: 100%;
            max-width: 400px;
            text-align: center;
        }

        .login-container h2 {
            margin-bottom: 20px;
            font-size: 28px;
            color: #333;
        }

        .form-control {
            width: 100%;
            padding: 12px;
            margin-bottom: 15px;
            border-radius: 5px;
            border: 1px solid #ccc;
            font-size: 16px;
        }

        button {
            width: 100%;
            padding: 12px;
            background-color: #4CAF50;
            color: white;
            font-size: 18px;
            border: none;
            border-radius: 5px;
            cursor: pointer;
            transition: background-color 0.3s ease;
        }

        button:hover {
            background-color: #45a049;
        }

        p {
            margin-top: 15px;
        }

        p a {
            color: #333;
            text-decoration: none;
            transition: color 0.3s ease;
        }

        p a:hover {
            color: #4CAF50;
        }

        /* Right-side image */
        .image-container {
            display: block; /* Show image on larger screens */
            max-width: 400px;
        }

        .image-container img {
            width: 100%;
            height: auto;
            border-radius: 10px;
        }

        @media (min-width: 0px) and (max-width: 760px) {
    .login-wrapper {
        display: flex;
        align-items: center;
        justify-content: center;
        flex-direction: column;
        box-shadow: 0px 0px 0px 0px white;
        
    }

    .login-container {
        width: 100%;
        max-width: 778px;
        text-align: center;
        display: flex;
        flex-direction: column;
    }

    .login-container h2 {
        margin-bottom: 20px;
        font-size: 35px; /* Consider adjusting this for smaller screens */
        color: #333;
    }
    body{
        background-color: white;
    }

    .head {
        font-size: 2rem; /* This might be too large for mobile, consider adjusting */
    }

    button {
        padding: 15px;
    }

    .form-control {
        padding: 15px; /* Adjust padding for mobile comfort */
    }

    /* Consider removing margin-top from body for a cleaner layout */
    body {
        padding-top: 30px; /* Use padding instead of margin for better control */
        padding-bottom: 30px; /* Maintain spacing at the bottom */
    }
}
        @media (min-width: 768px) {
            .image-container {
                display: block; /* Show image on larger screens */
            }
        }
    </style>
</head>
<body>
    <div class="login-wrapper">
        <div class="login-container">
            <form action="/login" method="post" class="needs-validation" novalidate>
                <h2>Login</h2>
                <div>
                    <label for="username" class="form-label">Username</label>
                    <input name="username" id="username" class="form-control" type="text" required />
                </div>

                <div>
                    <label for="password" class="form-label">Password</label>
                    <input name="password" id="password" class="form-control" type="password" required />
                </div>

                <button type="submit">Login</button>
                <p style="color: #4CAF50;">Not Have Account <a href="/userRegister">SignUP</a></p>
            </form>
        </div>

        <!-- Right-side image -->
        <div class="image-container">
            <img src="/photo/elelog.jpg" alt="Electrician">
